---
title: Steam 游戏破解与联机补丁指南
date: '2026-08-20T23:30:52+08:00'
updated: '2026-08-20T23:30:52+08:00'
categories:
- - 科技&产品
tags:
- Steam
- Pirated Game
- Epic
- 游戏
- 破解
description: Windows 下 Steam 游戏补丁与联机修复的完整流程：Unsteam 方案（在线联机）与 GBE Fork 方案（离线/局域网），以及 EOS 联机的配置方法。
---

{% box color:blue 核心方案思路 %}
Steam 游戏的破解与联机修复思路主要分两类：
1. **在线联机需求**：使用 **Unsteam**（伪装免费占位游戏借助 Steam 联机）。
2. **单机/局域网需求**：使用 **GBE Fork**（完全离线独立运行模拟器）。
3. **跨平台 EOS 联机**：当游戏自带 Epic Online Services 时，配合 **Nemirtingas Epic Emulator** 处理 EOS 鉴权层。
{% endbox %}

## 前置准备

- [Unsteam](https://cs.rin.ru/forum/viewtopic.php?f=29&t=134707) —— 联机补丁（需 Steam 客户端运行）
- [GBE Fork](https://github.com/Detanup01/gbe_fork) —— 离线/局域网模拟器
- [Steamless](https://github.com/atom0s/Steamless) —— SteamDRM 移除工具
- [Nemirtingas Epic Emulator](https://cs.rin.ru/forum/viewtopic.php?f=29&t=105551) —— EOS 联机模拟器

干净的 Steam 游戏文件可在 [CS.RIN.RU](https://cs.rin.ru/forum) 获取，获取方式见文末「工具与资源」。

### 查找 Steam AppID

AppID 是游戏在 Steam 商店的编号，两个方案都用得到（Unsteam 的 `real_app_id`、GBE 的 `steam_appid.txt`），先查好备用。

* **方法一**：打开游戏的 Steam 商店页，URL 里 `/app/` 后面那串数字就是。比如 `store.steampowered.com/app/3683770/` 的 AppID 是 `3683770`。
* **方法二**：在商店页地址栏直接搜索游戏名，或用 [SteamDB](https://steamdb.info) 按游戏名查。

---

{% tabs crack_plan, active:1 %}

<!-- tab 方案1：Unsteam（在线联机） -->
Unsteam 让游戏伪装成 Steam 的免费占位标题 **Spacewar（AppID 480）** 运行，借 Steam 的官方网络联机。**需要 Steam 客户端登录**；联机对象不限破解玩家。

#### 1. （可选）用 Steamless 去 DRM
只有游戏 exe 带 Steam Stub（SteamDRM 包装）才需要这一步。打开 `Steamless.exe`，拖入游戏可执行文件，点 **Unpack File**。
- 提示 `This file dose not appear to be packed with steamstub!` 说明没有 Steam 加密，跳过。
- 成功会生成 `游戏名.unpacked.exe`。把原 exe 改名备份（如 `游戏名.exe.old`），再把 unpacked 版改名为原 exe 名。

#### 2. 部署 Unsteam 并配置
把 `unsteam/{x64,x86}/*`（一般是 x64）复制到游戏目录，运行 `unsteam_loader64.exe`，生成 `unsteam.ini`。关键配置如下：

```ini
[loader]
exe_file=Game.exe            ; 【必改】游戏可执行文件名
dll_file=unsteam64.dll

[game]
offline_mode=0               ; 0 = 在线，1 = 强制离线
steam_id=
player_name=YourName         ; 默认是打包者的名字，建议改掉
real_app_id=replace_that     ; 【必改】游戏的 Steam AppID，获取方法见「前置准备」
fake_app_id=480              ; Spacewar，不用改
language=english
beta_name=public
saves_path=saves
game_launch_options=
```

#### 3. （可选）替换 EOS SDK
存在 `EOSSDK-Win64-Shipping.dll` 时，说明游戏用 EOS 联机，按「EOS 联机」标签页处理。

#### 4. 启动与验证
确保 Steam 客户端已登录，两种方式任选：
1. 直接运行游戏可执行文件。失败一般是 unsteam 版本架构不对，换 x64/x86。
2. 用 `unsteam_loader.exe` 启动。

{% mark green 成功标志 %}：Steam 个人状态栏显示正在玩 **Spacewar**。
<!-- endtab -->

<!-- tab 方案2：GBE Fork（离线 / 局域网） -->
GBE Fork 直接替换游戏的 `steam_api(64).dll`，让游戏以为 Steam 在运行。**不依赖 Steam 客户端，但无法在线公网匹配**，只能局域网或同配置玩家间联机。

#### 1. （可选）用 Steamless 处理 exe
同方案1第 1 步：Steamless 去掉 exe 上的 SteamDRM 包装。没 DRM 的文件安全跳过，不会损坏。

#### 2. 查找 steam_api(64).dll
在游戏目录搜索 `steam_api*.dll`，两种情况：
* **只有一个**（通用/Unity 引擎）：替换这个 DLL，`steam_settings` 放它旁边。
* **有两个**（Unreal Engine 游戏）：
```
<GameDir>\Engine\Binaries\ThirdParty\Steamworks\Steamv157\Win64\steam_api64.dll
<GameDir>\<GameName>\Binaries\Win64\steam_api64.dll
```
{% box color:yellow 注意 %}
UE 引擎启动时从 **Engine 目录**加载 `steam_api64.dll`，不是游戏主目录。只替换 Engine 目录那个，`steam_settings` 也放 Engine 目录。游戏主目录的 DLL 保持原版不动。
{% endbox %}

#### 3. 备份原版 DLL 并替换
1. 复制要替换的 DLL，重命名成 `steam_api64.dll.old`（后续生成接口文件备用）。
2. 从 GBE Fork 发布包（`emu-win-release`）复制对应架构的 DLL 覆盖：
   - 64 位游戏：`regular\x64\steam_api64.dll`
   - 32 位游戏：`regular\x86\steam_api.dll`
   *(模拟器 DLL 约 7–11MB，原版约 200–400KB，大小一眼能区分)*

#### 4. 创建配置目录与 AppID
1. 在模拟器 DLL 同级建 `steam_settings` 文件夹。
2. 新建 `steam_settings\steam_appid.txt`，只写纯数字 AppID。

#### 5. 生成 steam_interfaces.txt（推荐）
接口版本不匹配会导致崩溃。用 GBE Fork 里的 `generate_interfaces_x64.exe` 对**备份的原版 DLL** 跑一次，将生成的 `steam_interfaces.txt` 移入 `steam_settings\`。

#### 6. （可选）配置 INI
`steam_settings.EXAMPLE\` 有模板，按需复制到 `steam_settings\` 并去掉 `.EXAMPLE` 中缀：
- `configs.user.ini`：`local_save_path=./saves`（将存档保存在游戏目录）、`account_name=Player`（改名字）
- `configs.main.ini`：`offline=0`
- `configs.app.ini`：`unlock_all=1`（解锁全部 DLC）
<!-- endtab -->

<!-- tab EOS 联机（Epic 模拟器） -->
游戏自带的正版 `EOSSDK-Win64-Shipping.dll`（通常十几 MB）会连接 Epic 服务器验证账号、所有权和 DLC。**Nemirtingas Epic Emulator** 把这些验证在本机模拟，绕过 Epic 登录与鉴权。

* **适用条件**：游戏目录存在 `EOSSDK-Win64-Shipping.dll`（如 `binaries/win64/`）。

#### 1. 备份并替换
搜索游戏内所有 `EOSSDK-Win64-Shipping.dll`，原版改名备份（如加 `.old` 后缀），再把 Nemirtingas 的 DLL 复制到原路径覆盖。不确定加载哪份就全部替换。

#### 2. 启动生成配置
直接启动游戏，模拟器会在游戏工作目录自动生成配置文件 `NemirtingasEpicEmu.json`。

#### 3. 修改配置
用记事本打开 `NemirtingasEpicEmu.json`，填入自定义用户名：

```json
{
  "username": "MyAwesomeUsername"
}
```

#### 常用字段对照表

| 字段 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `username` | `DefaultName` | 你的名字，用于生成唯一身份 ID，联机不串号 |
| `appid` | 自动/留空 | 匹配同款游戏的房间，房主与玩家需一致 |
| `savepath` | 空（游戏目录） | 存档/成就保存位置；填 `appdata` 则存到 `%appdata%` |
| `disable_online_networking` | `false` | `true` 时禁用公网连接（仅局域网），崩溃则改回 `false` |
| `unlock_dlcs` | `false` | `true` 时尝试解锁全部 DLC |
| `log_level` | `OFF` | 排障时设 `DEBUG`，查看详细日志 |
<!-- endtab -->

{% endtabs %}

---

{% folding color:cyan 排障与常见问题排查 %}
* **游戏崩溃或提示 EOS 初始化失败**：
  1. 确认替换了实际加载的那份 DLL（有多份就全换）。
  2. 删除 `NemirtingasEpicEmu.json` 让模拟器重新生成。
  3. `disable_online_networking` 改回 `false`。
  4. 将 `log_level` 改为 `DEBUG` 查看详细运行日志。
* **查 EOS 游戏 appid**：
  打开 [EpicData](https://epicdata.info/items/) 搜索游戏名，在条目 `releaseInfo` 的 `appId` 字段中获取。
* **多人联机身份冲突**：
  每个玩家填不同的 `username`。同一个人的多台电脑建议共用同一份配置文件，避免 ID 变动。
* **和其他 EOS 补丁冲突**：
  功能重叠的补丁二选一（本模拟器可完整替代 FreeTP 的 EpicFix）。
{% endfolding %}

---

## 工具与资源

### 自动化补丁工具
- **[ARMGDDN Autocracker](https://github.com/KaladinDMP/ARMGDDN-Autocracker)** —— 基于 GBE Fork，右键游戏文件夹一键自动打补丁
- **[SteamAutoCracker](https://github.com/BigBoiCJ/SteamAutoCracker)** —— 封装 Steamless + Goldberg，一键去 DRM 和替换 DLL

### 其他补丁工具
- **[GreenLuma](https://cs.rin.ru/forum/viewtopic.php?f=29&t=103709)** —— Steam 客户端补丁，支持在线联机（Windows 专用）
- **[SLSsteam](https://github.com/AceSLS/SLSsteam)** —— Steam 客户端补丁，支持在线联机（Linux 专用）
- **[WeMod Patcher](https://github.com/k1tbyte/Wemod-Patcher)** —— 游戏修改器 WeMod 破解工具

### 资源论坛
- **[CS.RIN.RU 论坛](https://cs.rin.ru/forum)** —— 破解工具与 STEAM 游戏资源的主论坛
- **[Rentry 游戏导航](https://rentry.org/pgames)** & **[Ripped Guide](https://ripped.guide/PC-Software/Games/#cracking-tools)**

---

> 原文作者：[Nite](https://www.nite07.com/zh-cn/)  
> 原文链接：[https://www.nite07.com/zh-cn/posts/game-crack-tutorial/](https://www.nite07.com/zh-cn/posts/game-crack-tutorial/)  
> 授权许可：[CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0/)
